# Nagi

This repository contains the Nagi project — a TypeScript + Vite web app.

## Structure

- `lovable/` — app source and components
- `src/` — entrypoints and routes

## Development

Install dependencies and run the dev server:

```bash
npm install
npm run dev
```

